<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fadi_pro.fadi_pro">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ParseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity -->
        <!-- android:name=".Signin" -->
        <!-- android:label="@string/title_activity_signin" > -->
        <!-- </activity> -->
        <activity
            android:name=".MngrVacsActivity"
            android:label="@string/title_activity_vaccinations" />
        <activity
            android:name=".AddVACsActivity"
            android:label="@string/title_activity_add_vacs" />
        <activity
            android:name=".AddAnimalActivity"
            android:label="@string/title_activity_add_vacs" />
        <activity
            android:name=".ClientManagement"
            android:label="@string/title_activity_client_management" />
        <activity
            android:name=".Management"
            android:label="@string/title_activity_management" />
        <activity
            android:name=".ClientMan"
            android:label="@string/title_activity_client_man" />
        <activity
            android:name=".SignUpActivity"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AnimalMngActivity"
            android:label="@string/title_activity_animal_mng"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
